"""cumcm.t2 —— 问题 2：第二个检测点的选择与候选区域（对应顶层入口 T2.py）。

只有一次测向可用（第一检测点 S1 及其示向度 θ1，含 ±1° 误差），要求给出第二个检测点 S2 的
**选择策略**与**候选区域**。本包的路线是"把不确定性摆到明面上，然后按最坏情况选点"：

    config.py    全部参数与物理常量（含"为什么 d ∈ [5, 1500] m"的依据）
    region.py    几何底座：两楔形之交（= 附录图 2 的定位四边形）的直径、源不确定集的极点、
                 第二检测点的可行域（保证第二次测向一定听得到的透镜），并用 shapely 抽样校核
    score.py     算法主体：源不确定集离散 → 可测性保证 → 最坏定位直径 J → 最优区域（弧带）
                 （含理论层接入：细网格 GDOP 快筛 + 精确复核的全局认证、准则对照）
    theory.py    文献判据层：两站纯方位测向的 Fisher 信息 / CRLB 误差椭圆 / GDOP / 几何稀释，
                 与本文精确集员判据逐项对照（Foy 1976、任叶童 2016、Chan & Ho 1994、Chen 2009）
    report.py    CSV 适合度表、JSON 结论与控制台报表
    plotting.py  成果图：适合度主图 + 文献判据对照图
    cli.py       命令行入口

    config ──> region ──> score ──> report / plotting / cli
                   └──> theory ──┘

与问题一（`cumcm.t1`）的关系：定位区域的口径完全一致（同一个 `TriangulationRegion`），
本包只是把它向量化到"成千上万个候选第二检测点各算一次"，并用它做精确性校核。与问题三/四的
关系：没有依赖（t3/t4 也各自独立实现了补测选点，那里用的是 Fisher 信息准则作为贪心指标；
本包把 Fisher/CRLB/GDOP 作为**对照与快筛**实现于 `theory.py`，选点仍以最坏情况直径最小化为准
—— 因为清除判据是 20 m 硬阈值，而文献的统计判据在本模型上系统性低估约三成）。

算法结论（缺省 S1 = 原点、θ1 = 0°）：最优点 S2* = (801, 605) m —— 距 S1 1004 m、偏示向度
+37.1°（±37° 两瓣镜像等价），最坏定位直径 134.08 m（相比只有一次测向时的 1800 m 缩小 13.4 倍）；
候选区域是以 S1 为心、半径 948~1004 m、方位 θ1 ± [25.0°, 40°] 的两条镜像弧带（合计 0.0207 km²）。
文献常用的 GDOP 判据与期望精度口径给出同一个区域（相距 < 20 m），故结论对判据选择不敏感。
"""

from cumcm.t2 import config  # noqa: F401   （保证常量随包可见，与 t3/t4 一致）

__all__ = ["config", "region", "score", "theory", "report", "plotting", "cli"]
