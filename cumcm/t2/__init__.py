"""cumcm.t2 —— 问题 2：第二个检测点的选择与候选区域（对应顶层入口 T2.py）。

只有一次测向可用（第一检测点 S1 及其示向度 θ1，含 ±1° 误差），要求给出第二个检测点 S2 的
**选择策略**与**候选区域**。本包的路线是"把不确定性摆到明面上，然后按最坏情况选点"：

    config.py    全部参数与物理常量（含"为什么 d ∈ [5, 1500] m"的依据）
    region.py    几何底座：两楔形之交（= 附录图 2 的定位四边形）的直径、源不确定集的极点、
                 第二检测点的可行域（保证第二次测向一定听得到的透镜），并用 shapely 抽样校核
    score.py     算法主体：源不确定集离散 → 可测性保证 → 最坏定位直径 J → 最优区域（弧带）
    report.py    CSV 适合度表、JSON 结论与控制台报表
    plotting.py  成果图：全域视图 + 放大视图 + 最坏情形几何内嵌图
    cli.py       命令行入口

    config ──> region ──> score ──> report / plotting / cli

与问题一（`cumcm.t1`）的关系：定位区域的口径完全一致（同一个 `TriangulationRegion`），
本包只是把它向量化到"成千上万个候选第二检测点各算一次"，并用它做精确性校核。与问题三/四的
关系：没有依赖（t3/t4 也各自独立实现了补测选点，那里用的是 Fisher 信息准则，这里用的是
最坏情况直径直接最小化）。

算法结论（缺省 S1 = 原点、θ1 = 0°）：最优点在最远基线上 —— 距 S1 约 1005 m、偏示向度
±37°，最坏定位直径 134 m（相比只有一次测向时的 1800 m 缩小 13.4 倍）；候选区域是以 S1 为心、
半径 948~1004 m、方位 θ1 ± [25.0°, 40°] 的两条镜像弧带（合计 0.0208 km²）。
"""

from cumcm.t2 import config  # noqa: F401   （保证常量随包可见，与 t3/t4 一致）

__all__ = ["config", "region", "score", "report", "plotting", "cli"]
