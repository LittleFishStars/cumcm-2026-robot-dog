# 问题四 计算结果：定向 + 全向混合干扰源的搜索与清除（确定性策略）

> 求解程序 `T4.py`（实现见 `cumcm/t4/`），绘图脚本 `T4_figures.py`（`cumcm/analysis/figures_t4.py`）。
> 全部结果由 `--practice` 离线演练模式自动拉起 `jammers-py` 模拟器产生；同一 `--seed` 逐字节可复现（见第 7 节）。
> 分层与复用：`cumcm/t4` 依赖公共层 `common`、问题一几何 `t1`，并**直接复用**问题三的补测选点模块
> `t3.probing` 与覆盖基点常量 `t3.config.SURVEY_CENTERS`（薄转发 / 常量复用，不复制，见 `cumcm/t4/probing.py`
> 与 `cumcm/t4/sweep.py`）。

## 1. 运行环境

| 项目 | 值 |
| --- | --- |
| 操作系统 | Arch Linux |
| Python | 3.14（`.venv`，Tsinghua 镜像） |
| 依赖 | numpy 2.5.3、shapely 2.1.2（定位区域）、matplotlib 3.11（绘图） |
| 模拟器 | `jammers-py`（本地离线复现，问题四场景 `problem_no=4`） |
| 队号 | 202614023005 |

## 2. 问题四与问题三的**唯一**差异

题目第三问（`T3.py`）求解"全向干扰源"：任何位置、距源 ≤ 接收半径都能听到，所以"听不到"必然意味着
**太远**——这是问题三 7 点覆盖（圆域内任意点到最近巡视点 ≤ 1000 m）与 `no_signal ⇒ 源在接收半径
之外`（定位区域的 `add_outside` 硬约束）两条设计的前提。

问题四引入**定向干扰源**：定向源只在"定向方向 ±90°"（全角 180°，含边界）内有信号，波束之外一律
返回 `no_signal`（官方引擎 `_in_directional_coverage`：`|p−g| ≤ receive_m` **且** `angle(g→p) ∈
[θ−90°, θ+90°]`；清除 `/clear` 只判距离、与方向无关）。因此本问题中：

> **"搜索不到"不再等价于"源太远"，还可能是方向不对** —— 机器狗站在定向源的波束背面，
> 即使近在咫尺也收不到信号（实测：定向源正北 300 m 得 `direction 270.8°`，正南 300 m 得
> `no_signal`，而正南 19 m `/clear` 仍 `success`）。

值此，问题三的两处核心设计**失效**，须结构性重做：

| 问题三设计 | 问题四为何失效 | 替换方案 |
| --- | --- | --- |
| 7 点覆盖：圆域内任意点到最近巡视点 ≤ 1000 m ⇒ 必能听到任意源 | 定向源**背对**所有 7 个巡视点时，即使距离 ≤ 接收半径也听不到（蒙特卡洛 200 万随机源实测：**7 点听到率仅 80.7%**，平均每 5 个源漏 1 个） | **复用 7 覆盖基点 + 12 均匀外圈点**（第 3 节，路程最小化） |
| `no_signal` ⇒ 源在检测点 1000 m 外（`add_outside` 硬约束） | `no_signal` 可能是方向不对，无法转成"圆盘外"约束 | 定位区域只用 `direction`/`near` 约束（`DirProbRegion`，第 4 节） |

定位/补测/清除等其余手法与问题三同源，直接复用（`t3.probing` 薄转发；`t3.config.SURVEY_CENTERS`
常量复用）。

## 3. 检测设计：原点 + 7 覆盖基点 + 12 均匀外圈点（"尽量减少路程"优化的轻量布局）

定向源在检测点 $p$ 处可被听到 ⟺

$$|p-g|\le R \quad\text{且}\quad (p-g)\cdot u(\theta)\ge 0,\qquad R\in[1000,1500]\ \text{m},\ \theta\in[0°,360°)$$

即检测区 = 半径 $R$ 的**半圆盘** $H(g,\theta,R)$，而 $g,\theta,R$ 全部未知。测量点集合必须
"尽量命中任意半圆盘"。按"尽量减少路程"目标优化的轻量布局（复用问题三）：

1. **7 个覆盖基点** = 问题三巡视站（`t3.config.SURVEY_CENTERS`，直接复用，全向覆盖保证）；
2. **12 个均匀方位外圈点**（半径 1850 m、方位间隔 30°）——补全定向源的"迎光面"；
3. **原点** = 起点全频道扫描（位置成本为零），合计 **20 个测量位置**。

**为什么外圈取 1850 m、12 个方位。** 定向源的迎光区 = 源前方的半径纵深层 [|g|, |g|+R]；源最远
半径 1770，故**任意贴边源**的迎光区内沿 = 1770 m——外圈设在其内侧 1850 m，对任何半径的源径向
偏差 < 80 m（原"两两中点外推 ×2.6"把多数点推到 2270 m，反而离内部半径源的迎光面太远，实测只剩
99.81%）。方位间隔 30° 时相邻外圈点最大距离 ≈ 1850·sin15° ≈ 479 m ≪ 接收半径，任何方向的源
都能被方向差 ≤ 15° 的外圈点听到。**里程**：20 个测量位置的最短开放路径 **17 019 m**（比
"7+21 中点外推"的 21 580 m 省 21%，还少 9 个位置）。

**听到率统计**（`cumcm.t4.sweep.verify_hearing_stats`，共 4 224 640 算例）：

| 校验 | 算例 | 漏 | 听到率 |
| --- | --- | --- | --- |
| 蒙特卡洛（真实随机场景，400 万） | 4 000 000 | 381 | 99.9905% |
| 贴边对抗（源贴 1770 m、波束径向 ±8° × 360 方位 × 3 档 R） | 43 200 | 0 | 100.000% |
| 精细对抗（g 半径 0~1770 × 48 方位 × 180 光束 × 3 档 R） | 181 440 | 107 | 99.9410% |
| **合计** | **4 224 640** | **488** | **99.9885%** |

听到率比"7+21 中点外推"（99.81%）高近两个数量级，且**贴边对抗 0 漏**（原布局 129 600 例漏
1359）。残余漏例 ≈ 内部半径源 + 波束朝外 + 方向恰落测量点族空隙（精细对抗 400 例量级）。这是
"非严格保证"布局的固有边界，论文按实测统计报告，不声称严格不漏。

## 4. 定位设计：定位区域不含 `no_signal` 硬约束

`DirProbRegion`（`cumcm/t4/regions.py`，继承问题一的 `TriangulationRegion`）只叠加两类可证明约束：

| 测量结果 | 约束 | 理由 |
| --- | --- | --- |
| `direction` | 源在"以检测点为心、1500 m"的圆盘**内** + ±1° 楔形 | 收得到 ⇒ 距源 ≤ 接收半径上界 1500 m |
| `near` | 源在"以检测点为心、5 m"的圆盘**内** | 5 m 内位置几乎确定 |
| `no_signal` | **不作为约束** | 可能是方向不对，无法转成"圆盘外"（问题四的关键差异） |

区域 = 楔形交 ∩ 目标圆域 ∩ 接收半径内包，全是凸集交，区域保持凸、不裂块；`direction` 约束让单条
射线也给出"有限的有界区域"（不再是贯穿圆域的无界长带），多视角交会照常收敛。

**homing 兜底的第四问修正**：终点逼近可能从波束背面开始（实测 `no_signal`），此时没有示向度可用。
修正：先朝该频道**测量点质心**（必在迎光侧：全部方向观测都来自迎光侧）以 16 m 步长回撤，采到
示向度后再正常逼近。半圆盘是凸集，从迎光侧向源逼近始终留在波束内，故最终必能贴上（≤ 20 m 清除）。

**清除动作的顺路补测（对"直径比较大"的频道）。** 定向源只有迎光侧能测到（多视角稀缺），而清除
阶段机器狗要依次走到各频道的估计点——这些"路过"的清除点是对其它未收敛频道补测新视角的好机会。
策略在**每次清除动作后**（机器狗已到清除点），对满足以下条件的未清频道各补测一次示向度：

* **直径比较大**：定位区域最小覆盖圆直径 > 200 m（半径 > 100 m，即远未达到 20 m 清除精度）；
* 示向度条数未满预算（< OBS_CAP = 4）；
* 当前位置距该频道估计点 ≤ 1500 m（更远必然无信号，理性跳过）。

新示向度进入该频道的 `direction` 楔形约束，立即缩小定位区域。效果：多视角交会把"还差一点的"
频道在清除途中就收窄——20 局平均每局顺路补测 2 次；整体测向 251 次/局（29 点布局 358 次），
虚拟时间 6850 s（8465 s），机制净收益为负成本（顺路补测反而省）。

**途中顺路清除（沿用问题三 `_inline_clear` 的机制）。** 机器狗每从本站走到下一站，都把
「本站→圆心」与「下一站→圆心」两条连线夹出的方位扇区内的未清频道估计点**顺路走到试清**
（起点段退化为"朝向第一站 ±2°"的本地扇形；距圆心 600 m 以内的点留给阶段二专程处理）。
与问题三一致**不设"估计已够准"门槛**——扇区内都试，命中白捡 5 s、未命中只花 3 s，且命中后
该频道后续测量点都不再测向（扫描每个点本来就跳过 `cleared`）。这是"顺路清除"的实体：20 局
平均每局途中顺路清理 **7.8 个**（合计命中 157、未命中 72），收尾阶段的专程清除因此只剩少量
（`try` 一刀中从 244 降到 86），而总成本几乎不变（虚拟 6850 s vs 不加时的 6893 s、里程 26 242 vs
26 501 m）——顺路清除把"专程跑一趟"变成"路过顺手清"，净收益为正。

## 5. 演练结果（20 局，seed 0–19，问题四场景）

| 指标 | 值 |
| --- | --- |
| 局数 / 源总数 | 20 局 / **256 个源**（10~16 个/局，全向 + 定向混合） |
| **清除比例** | **256 / 256 = 100%（20/20 局全部清完，0 失败）** |
| 平均虚拟总耗时 | 6850 s（≈ 114 min；逐局 5615~8270 s） |
| 平均里程 | 26 242 m（其中扫描 17 019 m） |
| 平均测向次数 | 251 次/局（扫描"未听到频道"测向 + **清除时顺路补测**均值 2 次 + **途中顺路清除命中**均值 7.8 个 + 文献准则补测均值 2 次） |
| 定位误差（清除点距真值） | 均值 **7.27 m**，最差单源 19.80 m（全部 ≤ 20 m 清除半径） |
| 清除方法分布 | 途中顺路清除命中 157（61%）、`try` 一刀中 86、`homing`（兜底逼近）12、`try-refined` 1；无 multi / 无 failed |
| 听到率实证 | 256 个源全部在扫描第 ≤ 19 个测量位置内首次听到（共 20 个位置） |

逐局明细（seed : 清除数/源数（虚拟秒））：

```
seed0 =16/16(7281s)   seed1=11/11(7277s)   seed2=16/16(8129s)   seed3=11/11(6510s)
seed4 =11/11(5615s)   seed5=14/14(6305s)   seed6=16/16(8270s)   seed7=12/12(6718s)
seed8 =11/11(6734s)   seed9=13/13(7409s)   seed10=14/14(5750s)  seed11=13/13(6742s)
seed12=13/13(7288s)   seed13=12/12(7002s)  seed14=10/10(6626s)  seed15=11/11(5982s)
seed16=12/12(5664s)   seed17=14/14(7086s)  seed18=11/11(7533s)  seed19=15/15(7070s)
```

**"方向不对"的实证**（seed 0 频道 6）：该源在第 0~19 个测量位置的多次测量中全部 `no_signal`
（源就存在，距离未必超限，只是始终站在波束背面），直到扫描后期从正确方向命中才首次听到，随后
一次清除成功。没有外圈测量点的话这个源会被当成"不存在"漏掉。

**时间成本的说明。** 问题四虚拟总耗时（≈ 115 min）高于问题三（≈ 70 min）：17.0 km 的扫描行驶是
"覆盖任意定向源迎光面"的代价（比 37 点拖网的 26.9 km 省 37%、比 29 点布局的 21.6 km 省 21%）。
题目时限约束的是**现实时钟**（测试窗口 25 min / 程序 20 min）与虚拟 100 h；模拟器即时演算，
115 min 虚拟时间在现实秒级内跑完，不构成超时。

## 6. 图表（`figures/`，`T4_figures.py` 生成，均不占现实时间预算）

| 文件 | 内容 |
| --- | --- |
| `fig_t4_sweep_lattice.pdf` | 扫描布局：原点 + 7 覆盖基点 + 12 均匀外圈点，及半圆盘 ⊇ 内切圆示意 |
| `fig_t4_directional_concept.pdf` | "方向不对"示意图：迎光侧 `direction` / 背光侧 `no_signal` |
| `fig_t4_episode_clear.pdf` | 20 局清除数（256/256）与虚拟时间 |
| `fig_t4_first_heard.pdf` | 听到率实证：256 个源首次听到的扫描步骤分布（全部 ≤ 19） |
| `fig_t4_localization_error.pdf` | 清除点定位误差（均值 7.27 m，全部 ≤ 20 m） |
| `fig_t4_cost.pdf` | 时间构成：17.0 km 扫描行驶为固定搜索成本 |
| `results/t4/trajectory/epXX_seedM.png` + `.csv` | 每局总轨迹图与轨迹表（最新一局） |
| `results/t4/scan/epXX_sKK_*.png` | 每步扫描结果图（最新一局，20 张） |

## 7. 复现

```bash
python T4.py --plan-only                     # 扫描方案 + 听到率统计（不连模拟器）
python T4.py --practice 20 --seed 0          # 20 局演练（同 seed 逐字节可复现）
python T4.py                                 # 官方模式：连 http://127.0.0.1:2026
python T4_figures.py                         # 从 results/t4/ 生成论文图表
```

分层与依赖：`cumcm/t4` → `common` + `t1` + `t3.probing`（薄转发）+ `t3.config.SURVEY_CENTERS`（复用）。
接口调用逐次落盘 `results/t4/api_calls.jsonl`（20 局 5642 次调用，官方模式唯一证据链）。

## 8. 结果文件清单（`results/t4/`）

| 文件 | 内容 |
| --- | --- |
| `t4_sweep_plan.json` / `t4_sweep_points.csv` | 扫描方案（20 个测量位置、访问顺序、里程、听到率统计） |
| `t4_survey.json` | 20 局逐局统计（含真值核对与首次听到位置） |
| `t4_observations.csv` | 逐条观测明细（频道/坐标/结果/示向度/阶段） |
| `api_calls.jsonl` | 逐次接口调用（含原始响应） |
| `trajectory/`、`scan/` | 轨迹图与逐步扫描图（最新一局） |