"""问题四：定向 + 全向混合干扰源的搜索与清除，策略是确定性的

题目物理设定和公共层都跟问题三共用，模拟器客户端、演练场、几何、路由、出图都在 common 下。
定位、补测、清除的底层手法也同源于 t1 与 t3；唯一真正不同的地方是定向源只在 ±90° 波束内
有信号，方向不对就听不到，围绕它做了三处结构性改动。

检测就靠扫描布局，见 t4.sweep：问题三那 7 点覆盖只对全向源成立，定向源有背光问题，于是复用
问题三的 7 个覆盖基点，外圈再取 12 个均匀方位的 1850 m 点，起始角 7.5° 是为了跟内圈错开，
一共 20 个测量位置，访问里程 16 995.65 m。实测听到率 99.9891%，4 224 640 个算例漏 460，
其中 400 万蒙特卡洛漏 320、精细对抗漏 140、贴边对抗 0 漏；这是个轻量布局，不给严格保证，
残余漏例集中在内部半径源加波束朝外、方向恰好落进基点方位空隙，约占 0.011%，论文按实测
统计报告即可。

定位区域不吃 no_signal 硬约束，见 t4.regions.DirProbRegion："收不到"可能是方向不对，不等于
源在接收半径之外，所以只用两类约束，direction 给楔形加接收半径内包，near 给 ≤ 5 m 圆盘
内包，全是凸集求交，区域保持凸。

homing 兜底要抗波束背面，见 t4.strategy：兜底逼近的起点若恰好落在波束背面，实测就是没信号，
那就先朝该频道测量点质心回撤，质心必在迎光侧，然后再逼近，半圆盘是凸的，从迎光侧朝源逼近
一路都留在波束内。

分层上，本包只依赖 `common`、`t1` 和 `t3.probing`，t1 是定位区域的几何底座，t3.probing 管
补测选点，两题的定位几何与常量完全一致，直接复用而不是抄一份。
"""

from t4 import config  # noqa: F401   让结果目录一类常量随包可见